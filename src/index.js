import React, { Component } from 'react' 
import {
	Text,
} from 'react-native'

export default class App extends Component{
	render(){
		return (
			<Text style={{paddingTop: '10%'}}>Video Editor</Text>
		)
	}
}